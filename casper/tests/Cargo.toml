[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
casper-types = "1.5.0"
casperlabs-test-env = { version = "0.3.0" }
casper_types_derive = "0.1.0"
#casper-execution-engine = "2.0.0"
common = { path = "../did_common" }
